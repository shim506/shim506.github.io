### 12일
- Q. Single Activity 사용이 갖는 이점
    - 배경 : 새로운 탭이 추가되었고 Compose를 적극적으로 활용함. Activity 를 얼마나 사용할지를 고려함
    - A. 탭은 기존 MainActivity 에 이외 파생화면은 공통 Activity 위 Screen
    - A. 액티비티가 연쇄로 계속 생길 수 있다면 OOM(액티비티가 10개 이상 쌓이면 터지는 케이스 있음)
    - A. 진입점이 한개로 잡히면 공통 처리가 쉬워짐(스낵바 띄우기 등)
### 5일
- Snackbar & Toast
    - 스낵바
        - UI 에 종속됨(전체로 띄울 경우 커스텀 필요)
        - 메시지 + 사용자 액션(1개)
        - 제어 및 커스텀이 쉬움
    - 토스트
        - 앱 전역에서 쉽게 노출 됨
        - 메시지만을 노출함
        - 제어 및 커스텀 안됨

### 4일
- Q. uiState 와 Screen 에서 사용하는 상수 값은 어디 정의할 것인가?
- A. Viewmodel 에서 선언할 경우 uistate -> viewmodel 구조를 갖는다.(역전된 구조)
- A. uiState 에서 선언하고 Screen -> uiState 구조를 갖게 한다. 이는 기존 패턴에서 어긋나지 않는다. Screen 에서는 uiState 에 선언된 상태값을 참조하므로


### 3일
- Q. MVI 패턴에서 구체 콜백은 언제 사용되나
    - 배경: 콜백들은 Intent 로 전달되다 어느 단계부터는 구체 콜백으로 컴포넌트에 주입되는데 이 시점이 언제일까?
    - A: 공통 컴포넌트처럼 다른 곳에서 사용 가능성이 있다면 순수 함수로(구체 콜백 전달)  구성하고 그 외는 Intent 값을 받자

### 2일
- 디자인 시스템에서의 디자인 토큰 의미
    - 디자인에서 쓰이는 모든 "결정값"을 이름 붙인 변수로 관리하는 것
    - Primitive / Semantic / Component 순으로 구체서을 가짐
    - 실제값 hex 등 / color.text.link / button.primary.background
- 디자인 시스템의 가치
    - 컴포넌트와 토큰의 이름을 같게함으로써 소통이 쉬워진다
    - 디자인적 수정 가능성이 반영된 토큰을 공유함으로써 보다 쉽게 디자인이 변경될 수 있다
